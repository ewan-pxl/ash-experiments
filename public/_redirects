# Cloudflare Pages serves real files first (each /<slug>/ page, /assets/*),
# then falls back to the shell for everything else (/list and unknown paths).
# Root has no homepage of its own — bounce it to the OS front door at /home.
/ /home 302
/* /index.html 200
